import { createTheme } from '@mantine/core';

export const theme = createTheme({
  other: {
    adsrColors: {
      attack: '#51cf66',
      decay: '#339af0',
      sustain: '#ffd43b',
      release: '#ff6b6b',
    },
  },
});
